# Minimal dev configuration for the Redis container.
# Used for both the cache/pub-sub DB (0) and the ARQ queue DB (1).

appendonly yes
appendfsync everysec

maxmemory 256mb
maxmemory-policy allkeys-lru
